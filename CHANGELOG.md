## [Unreleased]

- MC-006: **Team logo image moderation (action + provider adapter)** — `internal.mediaAssets.getPendingTeamLogoAssetForModeration`; `internal.mediaAssetActions.runTeamLogoModeration` loads bytes with `ctx.storage.get`, calls `moderateImage` in `convex/lib/moderation/providers.ts` (`MODERATION_IMAGE_PROVIDER` `stub` \| `off` \| `sightengine`; `MODERATION_STUB_VERDICT`; `MODERATION_IMAGE_ON_FAILURE` `allow` \| `queue`; `MODERATION_IMAGE_REJECT_THRESHOLD`; Sightengine secrets `SIGHTENGINE_API_USER`, `SIGHTENGINE_API_SECRET`); `finalizeTeamLogoModeration` stores `moderationProvider` and structured `moderationResult`. **Docs:** [docs/content-moderation-and-language-filter.md](./docs/content-moderation-and-language-filter.md) §3.4–3.5. `bun run build` pass. Backlog MC-006.
- MC-004: **Publish-time text moderation (Convex actions)** — `convex/lib/moderation/textPublish.ts` (Perspective / Azure Content Safety / OpenAI Moderation / `stub`); `TEXT_MODERATION_PROVIDER`, `TEXT_MODERATION_REJECT_THRESHOLD`, `TEXT_MODERATION_ON_FAILURE` (`allow` \| `queue`); `api.textModeration.publishGate`; `api.publishTierListComment.submitTierListComment`; `api.publishTeamChatMessage.submitTeamChatMessage`; `tierListCommentInternal.ts`, `teamChatInsertInternal.ts`; shared heuristics `localCommentHeuristic.ts`; `tierListComments` + `teamChatMessages` store `textModerationProvider` / `textModerationResult`; public-comment + team-chat mutations throw when gate on so clients use actions. **Docs:** [docs/content-moderation-and-language-filter.md](./docs/content-moderation-and-language-filter.md) §1.3.1. **Files:** `convex/textModeration.ts`, `convex/publishTierListComment.ts`, `convex/publishTeamChatMessage.ts`, `convex/tierLists.ts`, `convex/teams/chat.ts`, `convex/teams/permissions.ts`, `src/components/teams/team-hub/chat-content.tsx`. `bun run build` pass. Backlog MC-004.
- MC-003: **Profanity display helper + surfaces** — `src/lib/moderation/profanity-display-text.ts`, `use-profanity-display-text.ts`; masks others’ UGC when `profanityFilterEnabled !== false` (same-length block chars). Tier lists (feed, browse, detail title/lanes), comments section, deck grid + gallery sidebar names, team deck tables, team chat. Own content stays raw. **Files:** listed in [docs/CHANGELOG.md](./docs/CHANGELOG.md). `bun run build` pass. Backlog MC-003.
- MC-002: **Profanity filter — Settings toggle** — `api.user.setProfanityFilterEnabled` patches `users.profanityFilterEnabled`; Settings “Community content” / “Filter strong language in community content” with display-vs-publish copy ([content-moderation-and-language-filter.md](./docs/content-moderation-and-language-filter.md) §1.1: **display** on your device vs **publish** / server checks later). `useOptimistic` + `useTransition`, Sonner success/error, optimistic revert on mutation failure. **Files:** `convex/user.ts`, `src/app/(app)/settings/settings-page-client.tsx`. `bun run build` pass. Backlog MC-002.
- MC-001: **Profanity filter preference (schema + API)** — `users.profanityFilterEnabled` optional boolean; `publicUserFromDocument` in `convex/validators.ts` sets effective value `profanityFilterEnabled !== false` (default on). `userValidator` includes required `profanityFilterEnabled: boolean`. `convex/user.ts` `currentUser` / `getById` return normalized user; `convex/tierLists.ts` feed and detail `author` paths use the same shape. [content-moderation-and-language-filter.md](./docs/content-moderation-and-language-filter.md) §1.2, §5.1 checklist. `bun run build` pass.
- TM-012: **Team logo (moderated pipeline)** — Schema: `mediaAssets` (`kind` `team_logo` \| `profile_avatar`, `storageId`, `status` pending \| approved \| rejected \| `needs_review`, moderation fields, indexes `by_status`, `by_teamId`, `by_teamId_and_kind`, `by_kind_and_status`); `teams.logoAssetId` optional, set only when moderation finalizes **approved** (`convex/mediaAssets.finalizeTeamLogoModeration`). Upload: `generateTeamLogoUploadUrl` + `submitTeamLogoUpload` (`edit_team_settings`), scheduled `internal.mediaAssetActions.runTeamLogoModeration`; stub / pluggable image moderation in `convex/lib/moderation/providers.ts` (see **MC-006** for `MODERATION_IMAGE_PROVIDER` and real providers). Read: `teams/logo.getTeamLogoPresentation` resolves `displayUrl` only for **approved** `logoAssetId` (plus legacy `imageStorageId`); captains see pending / rejected / `needs_review` copy per [content-moderation-and-language-filter.md](./docs/content-moderation-and-language-filter.md) §3.6. UI: `team-hub/team-logo-section.tsx`, overview wiring. Team dissolve deletes `mediaAssets` rows + storage. **Acceptance:** rejected uploads never update `logoAssetId` or appear in `displayUrl`. **Files:** `convex/schema.ts`, `convex/validators.ts`, `convex/mediaAssets.ts`, `convex/mediaAssetActions.ts`, `convex/teams/logo.ts`, `convex/teams/permissions.ts` (dissolve), `src/components/teams/team-hub/{team-logo-section,overview-panel,page-content}.tsx`. `bun run build` pass.
- TM-011: **Deck builder collaboration (§5.8–5.9, §8)** — Schema: `deckBuilderSessions` (per-deck shared `uiState` v1: `rightSidebarAction`, `galleryFilters`, `activeDeckSection`; `updatedAt`, optional `deckRevision`) and `deckPresence` (per-tab `sessionId`, palette `color`, `label`, viewport-normalized `cursor`, `lastSeenAt`). Convex `teams/deckCollaboration`: `getBuilderSession`, `listPresence` (empty unless `team_editable` + viewer can read deck), `patchBuilderUiState`, `presenceHeartbeat`; internal `pruneStaleDeckPresence` on a 2-minute cron. Client: `DeckCollaborationProvider` under `DeckDetailsProvider` subscribes only when `isTeamEditableDeck`; debounced `uiState` sync + remote apply with `applyingRemoteRef`; pointer `requestAnimationFrame` batched heartbeat + 18s keepalive; fixed viewport overlay cursors for other sessions; local hide for self and rows older than 60s `lastSeenAt`. No session/presence queries for `team_viewable`. **Files:** `convex/schema.ts`, `convex/validators.ts`, `convex/teams/deckCollaboration.ts`, `convex/crons.ts`, `src/providers/DeckCollaborationProvider.tsx`, `src/providers/index.ts`, `src/app/(app)/layout.tsx`. `bun run build` pass.
- TM-010: **Team-editable deck revisions (§8.3)** — `decks.revision` (optional, bumped on each card/layout mutation). `addCard` / `removeCard` / `moveCard` / `updateCardLayout` / `deleteCardLayout` require matching `expectedRevision` when `teamCollaboration === "team_editable"`; mismatch throws `CONFLICT: …`. Mutations return `{ revision }`. New decks get `revision: 0`. Client: `ActiveDeckProvider` and `SiloedDeckProvider` pass expected revision, update from mutation results, toast + dismissible banner on conflict; `useDeckEditor` exposes `teamEditableWriteConflict` / `dismissTeamEditableWriteConflict`. Import/export applies diffs sequentially for `team_editable`. **Files:** `convex/schema.ts`, `convex/validators.ts`, `convex/lib/deckAccess.ts`, `convex/decks.ts`, `src/lib/deck/visibility.ts`, `src/providers/ActiveDeckProvider.tsx`, `src/lib/deck/siloed-deck-context.tsx`, `src/lib/deck/use-deck-editor.ts`, `src/components/deck/team-editable-write-conflict-banner.tsx`, `src/components/deck-details/deck-details-view.tsx`, `src/components/gallery/active-deck-sidebar/sidebar.tsx`, `src/components/deck-details/deck-details-sidebar-import-export/hook.ts`. `bun run build` pass.
- TM-009: **Teams — aggregate “Team decks”** — `convex/teams/teamDecks.ts` adds `listAggregatedForMyTeams` (per-team `view_team_decks`, collaboration filter aligned with `listForHub` / §6). `/teams/decks` lists all readable team decks across the user’s teams; `TeamsAreaNav` on `/teams` and `/teams/decks` switches between “My teams” and “Team decks”; hub links to each team, optional team filter, table with deck / team / mode / owner. **Files:** `src/components/teams/teams-area-nav.tsx`, `teams-decks/content.tsx`, `teams-index/content.tsx`, `src/app/(app)/teams/decks/page.tsx`. `bun run build` pass.
- TM-007: **Team hub — members & decks** — `convex/teams/members.ts` (`listForHub` with `view_team`, `updateMemberRole` with `assign_roles`); `convex/teams/teamDecks.ts` (`listForHub` with `view_team_decks`, filters All / `team_viewable` / `team_editable` for §6). UI: `shell-content` tabs Overview / Members / Decks; `members-content.tsx` (table, `assign_roles` → role `Select` for non-captain rows, read-only for others); `decks-content.tsx` (table + collaboration toggle, links to `/decks/[deckId]`, empty state + `create_team_deck` CTA). Routes `/teams/[teamId]/members`, `/teams/[teamId]/decks`. API: `api.teams.members`, `api.teams.teamDecks`. `bun run build` pass.
- TM-006: **Team hub** — `/teams` lists the signed-in user’s teams (`api.teams.permissions.listMyTeams`); `/teams/[teamId]` hub uses `getTeam` in layout metadata + shell (overview tab: name, description, `UsersRound` placeholder logo until TM-012). Playbook-style folders: `src/components/teams/team-hub/` (`hook.ts`, `shell-content.tsx`, `page-content.tsx`, `overview-panel.tsx`), `src/components/teams/teams-index/`. Shared nav builder `src/components/shell/main-nav-build.ts`: **Teams** link (desktop left sidebar + mobile profile sheet) only when the user has at least one team. Client/API references use typed `api.teams.permissions` (same module as `api["teams/permissions"]`).
- TM-005: Team deck **write path** §7.3 — `requireDeckWriteAccess` / `canUserWriteDeck` in `convex/lib/deckAccess.ts` (`team_viewable`: owner-only; `team_editable`: owner or `create_team_deck` on `deck.teamId`). All `convex/decks.ts` mutating paths use the helper instead of `deck.userId` alone. `canViewDeck` typed for query or mutation context; `sessions.setActiveDeck` allows any deck the user can view (e.g. team-readable). Tests: `tests/deck-access-write.test.ts`.
- TM-004: Decks **team visibility** — schema adds optional `teamId`, `teamCollaboration` (`none` \| `team_viewable` \| `team_editable`), extends deck `visibility` with `team`; index `by_teamId`. `deckValidator` and `convex/lib/deckAccess` team read path (`getById` / `canViewDeck` via `getEffectiveTeamRole` + `view_team_decks`). `listPublic` still uses `by_isPublic` then narrows to `public` \| `tournament` so `isPublic`/gallery behavior matches pre-change. `create` / `update` accept team fields with `create_team_deck` checks. Migration `migrations/migrateDeckTeamFields.backfillDeckTeamFields` sets `teamCollaboration: "none"` on legacy rows. UI labels/icons for `team` visibility; team hidden from owner dropdowns until a team picker exists (meta panel shows Team only when the deck is already team).
- TM-002: `convex/teams/permissions.ts` — capabilities per `docs/teams-feature-implementation.md` §4.2 (`hasCapability`, `ALL_TEAM_CAPABILITIES`, `getActiveMembership`, `getEffectiveTeamRole`, `requireCapability`, `requireCaptain`); mutations `create`, `updateSettings`, `dissolve`; queries `getTeam`, `listMyTeams` (signed-out → `[]`). Tests: `tests/teams-permissions.test.ts`. Codegen: `api.teams.permissions` / `api["teams/permissions"]`.
- TM-003: Team invitations — `convex/teams/invites.ts`: `createInvite` (`invite_members`, SHA-256 `tokenHash`, optional email **or** `invitedUserId`, no `captain` role), `revokeInvite` (pending only), `acceptInvite` (email or `invitedUserId` match, expiry + accepted checks, `teamMembers` insert or reactivate `removed`). Route `/teams/invite/[token]` + email stub [teams-invite-email-stub.md](./docs/teams-invite-email-stub.md). API: `api.teams.invites`.
- TM-001: Convex schema for teams — `teams` (optional `slug`, `captainUserId`, `imageStorageId`, indexes `by_slug`, `by_captainUserId`), `teamMembers` (role union, `active`/`removed`, indexes `by_teamId_and_userId`, `by_userId`, `by_teamId`), `teamInvites` (hashed `tokenHash`, optional `email`/`invitedUserId`, indexes `by_teamId`, `by_tokenHash`, `by_email_and_teamId`). Document validators in `convex/validators.ts`; migration helper `migrations/migrateTeamsCore.teamsCoreMigrationStatus` (`convex/migrations/migrateTeamsCore.ts`). No UI.
- RSC-008: On `gallery`, `deckDetails`, and `admin`, the app layout eagerly `import()`-prefetches the same `@/components/shell` `RightSidebar` chunk used by `next/dynamic`, so the first icon-rail expand avoids paying dynamic-import latency after slot registration (`src/app/(app)/layout.tsx`).
- RSC-006: `useRegisterSlot` uses `useEffectEvent` plus a layout effect that commits the latest `Component` / `options` each time, and a separate layout effect whose only job is `unregister` when `area` or `id` changes (or on unmount), so unstable function identities no longer trigger an unregister/reregister cycle (`src/components/shell/shell-slot-provider/use-register-slot.ts`).
- RSC-001: Right sidebar expanded panel no longer animates `width` with Framer; layout width toggles in one step while content enters on a composited `x` transform (`src/components/shell/right-sidebar/expanded-panel.tsx`), reducing per-frame flex reflow against the gallery virtualizer. Honors `prefers-reduced-motion` and keeps resize drags at zero-duration motion.
- RSC-002: `ShellSlotProvider` exposes split contexts — `useShellSlotSlots`, `useShellSlotSidebarWidth`, `useShellSlotActiveSidebarActionId`, `useShellSlotActions` — so `GalleryFiltersProvider` (active action only), app layout (slot registry only), deck-details sidebar gallery (width only), and `useRegisterSlot` (actions only) avoid re-rendering on unrelated shell updates; `useShellSlot()` still composes the full `{ state, actions }` shape (`src/components/shell/shell-slot-provider/*`, `src/providers/GalleryFiltersProvider.tsx`, `src/app/(app)/layout.tsx`, shell mobile/right-sidebar providers).
- CAT-008: Documented peak memory model for `GalleryFiltersProvider.meta.filteredCards` and the gallery `CardDetailsDialog` (`cards` prop shares the same array reference; no second full clone; catalog `CachedCard` graph dominates; lazy id/windowed navigation deferred pending profiling) — `docs/card-data-hooks.md` (`src/providers/GalleryFiltersProvider.tsx`, `src/components/universus/card-details/dialog.tsx`, `src/components/gallery/main-content/content.tsx` unchanged).
- CAT-007: Removed full-table `collect()` on `cards` in hot `cards.list` and related queries. **`list`** (empty search) and **`listReleased`** use bounded `paginate` loops; **`getRarities` / `getTypes` / `getSets`** read a singleton **`cardFacetSnapshot`** table maintained by **`rebuildCardFacetSnapshot`** in **`convex/cardFacets.ts`** (`runCatalogAggregateRefresh`, paginated scan). **`listCharacters`**, **`listPaginated`**, and **`getCardVariants`** use indexed or capped reads. **Admin** `releaseCards` and `completeIngestionJob` call `runCatalogAggregateRefresh` in-process; import/clear **actions** use `ctx.runMutation(internal.cardFacets.rebuildCardFacetSnapshot)`; `listUnreleasedCards` is capped. See `docs/CAT-007-manual-checklist.md` (`convex/schema.ts`, `convex/cards.ts`, `convex/cardFacets.ts`, `convex/admin.ts`).
- CAT-006: Versioned static full catalog (gzip JSON on R2/CDN keyed by `cardDataVersion`) — design note covering security (integrity hash, HTTPS, CORS vs same-origin proxy, parsing limits), CDN **`Cache-Control: public, max-age=…, immutable`** for versioned URLs, and **fallback** to existing paginated Convex sync on missing config or any fetch/verify failure — see `docs/implementation/notes/cat-006-versioned-static-catalog.md` (implementation deferred).
- CAT-005: Main-thread budget spike for gallery `filterCards` + `sortCards` — documented methodology, **defer** verdict (worker / client search index not needed yet at target catalog scale given existing `useDeferredValue` + single-pass filter + virtualized slice), bundle cost estimate for a hypothetical worker (~8–25 kB gzip + transfer caveats), **do not merge** implementation in this pass — see `docs/implementation/notes/cat-005-main-thread-budget-spike.md`.
- CAT-004: Removed module-level `lastSortCache` from `sortCards` in `use-universus-cards.ts`. Gallery and `useFilteredAndSortedCards` already memoize filter+sort by inputs; `sortCards` is now a pure function with no global cache (`src/lib/universus/use-universus-cards.ts`, `src/providers/GalleryFiltersProvider.tsx` pipeline unchanged).
- CAT-003: Catalog sync `loadProgress` uses `getCardDataVersion`’s `cardCount` (gallery-eligible total) instead of a hard-coded `3000`; caps in-flight pages below 100% when the total is known, hits 100% when the last `listReleasedPaginated` page completes, with a heuristic fallback when `cardCount` is unavailable (`src/lib/universus/use-universus-cards.ts`).
- CAT-002: Full catalog `fetchFromConvex` uses a named `GALLERY_CATALOG_SYNC_PAGE_SIZE` of **1000** for `listReleasedPaginated` (aligned default in `convex/cards.ts`); documented estimated rows and JSON size per chunk in `docs/card-data-hooks.md` (`src/lib/universus/use-universus-cards.ts`, `convex/cards.ts`).
- CAT-001: Full catalog sync uses `listReleasedPaginated` filtered with the same gallery rule as `listAllCardsChunked` / `filterCards` (`isGalleryCatalogCard`); IndexedDB stores only those rows. Dual-face backs are merged in memory after paginated fetch via new `cards.batchGetCardsByIds`. Cache metadata gains `schemaVersion` (`CARD_CACHE_SCHEMA_VERSION` in `card-store`) to invalidate pre-change blobs; `totalCards` counts gallery rows only (`use-universus-cards`, gallery filters, home stat). (`convex/cards.ts`, `src/lib/universus/card-store.ts`, `src/lib/universus/use-universus-cards.ts`, `src/providers/GalleryFiltersProvider.tsx`, `src/app/(app)/home/page.tsx`, `docs/card-data-hooks.md`, `docs/SYSTEM_ANALYSIS.md`).
- PERF-014: Gallery server-assisted search spike — `cards.galleryIndexedSearchSpike` query (full-text on `searchName` with `search_gallery_name` filters; set-scoped name prefix via `by_setCode_and_name`; set browse via `by_setCode_and_collectorNumber`); schema adds those indexes; migration path in `docs/gallery-server-search-spike.md` (`convex/schema.ts`, `convex/cards.ts`).
- PERF-013: Tuned `next/image` usage for gallery and deck surfaces: explicit `sizes` on thumbnails, deck cover strip, hero panel, and hover portals; `imagePriority` / `thumbnailPriority` / `coverImagePriority` for the first visible grid or list slice; dual-face card UIs use eager loading only for the visible face (`fetchPriority="low"` on the hidden face) in grid tiles, stack items, `CardDetailsV2`, and legacy card-details image panels; deck-details sidebar gallery grid passes priority for the first few tiles (`src/components/universus/card-grid-item/*`, gallery main content views, deck grid, deck-details card items and hero, hover preview portals).
- PERF-011: Code-split heavy routes with `next/dynamic` (`ssr: true`) and a shared centered spinner fallback (`RouteChunkFallback`): admin dashboard and sub-pages, `/community/tier-lists` + dynamic import of `CommunityTierListDetailView` on `/community/tier-lists/[tierListId]`, settings, and collection. Client implementations live in co-located `*-page-client.tsx` files where needed (`src/components/shell/route-chunk-fallback.tsx`, `src/app/(app)/admin/**/page.tsx`, `src/app/(app)/community/tier-lists/page.tsx`, `[tierListId]/page.tsx`, `settings/page.tsx`, `collection/page.tsx`).
- PERF-010: `AuthGuard` gates the loading skeleton on `useConvexAuth` only: skeleton while `isLoading`, and while `requireAuth` with a signed-out user until `router.replace("/sign-in")` runs; removed the unconditional 1s timeout and `showContent` state that caused arbitrary delay and could show app chrome before auth finished (`src/components/auth-guard.tsx`).
- PERF-008: `UIStateProvider` batches `persistUIState` with a short debounce and `requestIdleCallback` (fallback `setTimeout(0)`) so fast filter/scroll updates do not synchronously call `localStorage.setItem` on every React state tick; tab hide, `pagehide`, `beforeunload`, and provider unmount cancel pending work and flush the latest snapshot (`src/providers/UIStateProvider.tsx`).
- PERF-007: Active deck and siloed deck (details) providers drop per–catalog-swap `Map` rebuilds over all `cards`; add/move eligibility uses the shared `CardIndex.byId` from `useCardIndex()` so lookups stay O(1) per card id without duplicating the index work (`src/providers/ActiveDeckProvider.tsx`, `src/lib/deck/siloed-deck-context.tsx`).
- PERF-006: `CardDataProvider` splits card **catalog** and **reference** (formats/sets + lookups) into separate React contexts: `useCardCatalog`, `useCardReferenceData`, and `useCardIndex`; `useCardData` still merges both for existing call sites. Progressive `cards` array updates no longer invalidate reference-only subscribers. `GalleryFiltersProvider` and tier-list detail hook use the narrow hooks where appropriate; hook choice is documented in `docs/card-data-hooks.md` (`src/lib/universus/card-data-provider.tsx`, `src/providers/GalleryFiltersProvider.tsx`, `src/components/community/tier-lists/detail-view/hook.ts`).
- PERF-005: Card grid and deck stack tiles replace Framer `AnimatePresence` flip with CSS opacity crossfade; deck details stacked view removes per-index motion stagger; root `Providers` wrap the app in `LazyMotion` with `domAnimation` and `strict` while UI uses `m` from `framer-motion/m`; card details v1/v2 image areas and dialogs align with the same flip pattern; tier list pool panel, gallery loading bar, settings sections, and sidebar hover preview honor `prefers-reduced-motion`; shell nav drops `layoutId` in favor of a static active outline (`src/app/providers.tsx`, `src/components/universus/card-grid-item/image-stage.tsx`, `src/components/deck-details/**/*`, `src/components/shell/left-sidebar/nav.tsx`, plus Framer import migrations under `src/`).
- PERF-004: Gallery main content hosts one `CardDetailsDialog` (`GalleryMainContentBody`): grid, list, and details views call `onOpenCardDetails` so `CardGridItem` / `CardListItem` omit per-row dialogs when that callback is set. `CardDetailsV2` loads via `React.lazy` and `Suspense` when the dialog is open (`src/components/gallery/main-content/content.tsx`, `grid-view.tsx`, `list-view.tsx`, `details-view.tsx`, `card-list-item/*`, `card-details-list-item.tsx`, `universus/card-grid-item/*`, `universus/card-details/dialog.tsx`).
- PERF-002: Client catalog `filterCards` runs in one pass over the full local card list (replacing many chained `.filter` calls), precomputes attack/block zone lowercase lists, and optionally applies format legality in the same pass via `passesFormatLegality` (`CardDataProvider` / `getFilteredCards`). `sortCards` avoids per-comparison `switch` work with a comparator factory and skips sorting for 0–1 items (`src/lib/universus/use-universus-cards.ts`, `src/lib/universus/card-data-provider.tsx`).
- PERF-001: Gallery list filtering and sorting use a deferred pipeline in `GalleryFiltersProvider` (`useDeferredValue` on search, search mode, persisted filters, and sort; `startTransition` for persisted filter mutations). `meta.filteredListKey` aligns grid/list/details and deck-details sidebar slice keys with the deferred snapshot so typing stays responsive (`src/providers/GalleryFiltersProvider.tsx`, `src/components/gallery/main-content/content.tsx`, `src/components/deck-details/deck-details-sidebar-gallery/hook.ts`).
- DND-009: Mobile actions draggable drawer coalesces resize updates with `requestAnimationFrame` and applies shell, panel, and action-area heights imperatively during drag; React `drawerHeight` state syncs on pointer up, cancel, and settle (`src/components/shell/mobile-actions-sheet/draggable-drawer.tsx`).
- DND-007: Drag overlay prefers `DragItem.previewImageUrl` from the source tile’s in-DOM image (`currentSrc` / `src` via optional `previewImageRef` on `useTcgDraggable`); gallery grid, list, details, active deck rows, and deck-details gallery items attach refs to their `next/image` thumbnails. Overlay ghost still uses rAF positioning (DND-001); `HTMLImageElement.decode()` runs on the overlay `<img>` in a post-paint `requestAnimationFrame` to reduce first-frame decode contention without blocking pointer moves (`src/lib/dnd/tcg-dnd-provider.tsx`, `src/lib/dnd/use-tcg-draggable.ts`, draggable card components).
- DND-006: Tier list droppable lanes use `transition-shadow` and `transition-colors` instead of `transition-all`, drop the hover `scale` transform, and use a `ring` for drag-over emphasis so rapid `activeDropZone` updates do not thrash layout (`src/components/community/tier-lists/detail-view/lane-row.tsx`).
- DND-005: Stabilized `useTcgDroppable` dependencies at deck details and tier-list lane call sites: `deck-details-cards-section-model` uses `TCG_DND_ACCEPTS_CARD_ONLY`; `lane-row` uses module-level empty vs card-only accept lists and `useCallback` for lane drops (`src/components/deck-details/deck-details-cards-section-model.ts`, `src/components/community/tier-lists/detail-view/lane-row.tsx`).
- DND-004: While dragging, resolve `activeDropZone` from global `pointermove` / `mousemove` / `touchmove` via `document.elementFromPoint` and `data-tcg-drop-zone` on droppable roots, validated against the registered zone map (`src/lib/dnd/tcg-dnd-provider.tsx`, `src/lib/dnd/use-tcg-droppable.ts`); removed enter/leave as the source of truth for hover targeting.
- DND-001: Coalesced drag overlay positioning with `requestAnimationFrame` and imperative `transform` updates on a ref in `src/lib/dnd/tcg-dnd-provider.tsx` (no per-move React state for x/y).
- DND-002: Removed CSS `transition-transform` from the drag overlay wrapper in `src/lib/dnd/tcg-dnd-provider.tsx` so the ghost follows the pointer without easing lag.
- DND-003: Split TCG DnD into separate React contexts in `src/lib/dnd/tcg-dnd-provider.tsx` (stable actions vs drag state vs `activeDropZone`); `useTcgDraggable` uses `useTcgDndActions` only so card tiles do not re-render on drop-zone highlight changes. `useTcgDnd` still merges the full value for droppables and other consumers.
- RSC-005: Committed drop highlight uses a module-level `useSyncExternalStore` snapshot instead of `TcgDndProvider` React state, so pointer moves across zones do not re-render the provider subtree. `useTcgDroppable` uses `useTcgDndActions`, `useTcgDndDragState`, and per-zone `useTcgActiveDropZoneIsOver(id)` (`src/lib/dnd/tcg-dnd-provider.tsx`, `src/lib/dnd/use-tcg-droppable.ts`, `src/lib/dnd/index.ts`).

## 2026-01-30
- Phase 0: Added `ShellSlotProvider` registry in `src/components/shell/shell-slot-provider.tsx` to centralize top-bar and right-sidebar slot management and improve slot stability.
- Phase 1: Added `GalleryFiltersProvider` in `src/providers/GalleryFiltersProvider.tsx` and exported it from `src/providers/index.ts` to consolidate gallery filter state, actions, and derived metadata.
- Phase 2: Refactored gallery views and filters in `src/components/gallery/gallery-view.tsx` and `src/components/gallery/gallery-header.tsx` to consume `GalleryFiltersProvider` and register top-bar slots declaratively.
- Phase 3: Removed card grid prop drilling by wiring `CardGridItem` to `useActiveDeck` and simplifying `InfiniteCardGrid` in `src/components/universus/card-grid-item.tsx` and `src/components/gallery/gallery-view.tsx`.
- Phase 4: Migrated shell layout and header/sidebar components to slot registry usage in `src/components/shell/top-header.tsx`, `src/components/shell/right-sidebar.tsx`, `src/components/shell/mobile-actions-*`, `src/app/(app)/layout.tsx`, and `src/components/admin/admin-page-header.tsx` to eliminate action prop threading.
- Phase 5: Improved gallery accessibility in `src/components/gallery/gallery-header.tsx` and `src/components/universus/card-grid-item.tsx` with keyboard support, labels, and input attributes for interactive elements.
- Phase 6: Added reduced motion support via `src/lib/reduced-motion.ts` and applied it in `src/components/universus/card-grid-item.tsx`, `src/components/shell/left-sidebar.tsx`, and `src/components/gallery/gallery-view.tsx` to soften animations and limit transitions.
- Phase 7: Updated gallery loading and search placeholders in `src/components/gallery/gallery-view.tsx` and `src/components/gallery/gallery-header.tsx` to use ellipsis characters.
- Phase 8: Applied functional state updates and lazy UI state initialization in `src/providers/UIStateProvider.tsx` to reduce re-renders and avoid redundant hydration work.
- Phase 9: Updated provider composition in `src/app/(app)/layout.tsx` and `src/app/(app)/gallery/page.tsx` to scope gallery filters and use shell slot context at the app layout level.
- Phase 10: Removed deprecated shell and top bar contexts from `src/components/gallery/gallery-top-bar-context.tsx`, `src/components/admin/admin-top-bar-context.tsx`, and `src/components/shell/right-sidebar-context.tsx` after migration to slot registry.
- Fix: Wrapped gallery shell content with `GalleryFiltersProvider` in `src/app/(app)/layout.tsx` and removed the page-level wrapper in `src/app/(app)/gallery/page.tsx` so top-bar slots render under the provider.
- Fix: Stabilized `ShellSlotProvider` actions in `src/components/shell/shell-slot-provider.tsx` to prevent repeated slot registration loops.
