{
  "name": "uvs-decks-gg",
  "install": "export PATH=\"$HOME/.bun/bin:$PATH\"; command -v bun >/dev/null || curl -fsSL https://bun.sh/install | bash; export PATH=\"$HOME/.bun/bin:$PATH\"; bun install",
  "terminals": [
    {
      "name": "dev",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\"; bun run dev",
      "description": "Next.js app on port 8090"
    }
  ],
  "ports": [
    {
      "name": "Next.js",
      "port": 8090
    }
  ]
}
